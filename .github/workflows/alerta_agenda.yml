# .github/workflows/alerta_agenda.yml

name: Alerta de Agenda Diário (Cron Job)

# 1. GATILHO (CRON JOB)
on:
  schedule:
    # Roda diariamente às 10h, 14h, 18h e 22h UTC (corresponde a 7h, 11h, 15h e 19h no Brasil/GMT-3)
    - cron: '0 10,14,18,22 * * *' 

  # Permite que o fluxo seja acionado manualmente para testes
  workflow_dispatch: 

# ... (restante do arquivo permanece o mesmo) ...
